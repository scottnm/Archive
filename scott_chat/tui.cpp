#include "stdafx.h"

#include "tui.h"

// TODO(scottnm): remove this when you write the tui you bum
#include <assert.h>

namespace tui
{
    void init()
    {
        assert ("write the damn tui module" && false);
    }

    void write_to_input_field(char c)
    {
        (void)c;
    }

    void write_msg_to_conversation_thread(const char* msg)
    {
        (void)msg;
    }
}
